# Optional. The app remains fully usable in offline demo mode without this key.
OPENAI_API_KEY=

# Cost-efficient default for daily tutoring. Override if desired.
OPENAI_MODEL=gpt-5.6-luna

PORT=3000
